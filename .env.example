# --- App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- n8n webhook (Spark RD Station unificado)
N8N_WEBHOOK_URL=https://growthsparkmaxx.app.n8n.cloud/webhook/greatpages-rd-unificado
N8N_WEBHOOK_AUTH=5e623e29ed1173224c053ad8f376bd89

# --- E-mail corporativo
# CSV de domínios bloqueados (sobrescreve a lista default em lib/corporate-email.ts).
BLOCKED_EMAIL_DOMAINS=

# --- Rate limit (server-side) — submissões/min por IP
RATE_LIMIT_PER_MIN=10

# --- Supabase (opcional — backup de leads quando webhook falha)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# --- Cloudflare Turnstile (opcional — anti-spam invisível)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# --- PostHog (opcional — analytics de funil)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
